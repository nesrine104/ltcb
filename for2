#include<stdio.h>
#include<math.h>

int main() {
    int n;
    int a = 0;
    scanf("%d", &n);
    for (int i = 2; i <= sqrt(n); i++) {
        if (n%i == 0) 
            a++;
    }
    if (a == 0)
        printf("n la so nguyen to");
    else printf("n khong phai la so nguyen to");
    return 0;
    
}
