#include<stdio.h>

int main() 
{
    int arr[5]={1, 2, 3, 4, 5};
    printf("mang dao nguoc la: ");
    for (int i=4;;i--) {
        printf("%d ", arr[i]);
        if (i==0) break;
    }
    return 0;
}
