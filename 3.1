#include<stdio.h>
#include<math.h>

int main() 
{
    int a[10];
    for (int i=0; i<10; i++) {
        scanf("%d", &a[i]);
    }
    int s = 0;
    for (int i=0; i<10; i++) {
        s+=a[i];
    }
    printf("%d", s);
    return 0;
}
