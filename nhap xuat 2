#include<stdio.h>
#include<stdlib.h>
#include<math.h>

int main()
{
    int d, m, y;
    printf("Nhap ngay thang nam:");
    scanf("%d %d %d", &d, &m, &y);
    if(d>31||d<0||m>12||m<0||y<0) printf("Khong co");
    else 
        printf("%d/%d/%d", d, m, y);
    return 0;
}
