#include<stdio.h>

int giaithua(int n)
{
    if (n<=1) return 1;
    else return n*giaithua(n-1);
}
int main()
{
    int n;
    printf("Nhap n: "); scanf("%d", &n);
    if (n>=0)  printf("n!=%d", giaithua(n));
    else printf("Khong the tinh, moi chay lai\n");
    return 0;
}
