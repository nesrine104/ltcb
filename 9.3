#include<stdio.h>
void tim(int *m, int *n, int a[10],int *p)
{
    for(int i=0; i<10; i++) {
        if (*n>*(p+i)) *n = *(p+i);
        if (*m<*(p+i)) *m = *(p+i);
    }
}
int main()
{
    int arr[10];
    int *p;
    p=arr;
    
    for(int i=0; i<10; i++) {
        printf("a[%d]=", i);
        scanf("%d", arr+i);
    }
    int max=*p, min=*p;
    printf("Mang da nhap la: ");
    for(int i=0; i<10; i++) {
        printf("%3d", *(p+i));
    }
    printf("\n");
    tim(&max, &min, arr, p);
    printf("So lon nhat: %d\n", max);
    printf("So nho nhat: %d\n", min);
    return 0;
}
