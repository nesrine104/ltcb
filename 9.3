#include<stdio.h>

int main()
{
    int arr[10];
    int *p;
    p=arr;
    
    for(int i=0; i<10; i++) {
        printf("a[%d]=", i);
        scanf("%d", arr+i);
    }
    int max=*p, min=*p;
    printf("Mang da nhap la: ");
    for(int i=0; i<10; i++) {
        printf("%3d", *(p+i));
    }
    printf("\n");
    for(int i=0; i<10; i++) {
        if (min>*(p+i)) min = *(p+i);
        if (max<*(p+i)) max = *(p+i);
        
    }
    printf("So lon nhat: %d\n", max);
    printf("So nho nhat: %d\n", min);
    return 0;
}
