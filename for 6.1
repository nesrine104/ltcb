#include<stdio.h>

int main() 
{
    int a, b, ucln;
    printf("Nhap 2 so: ");
    scanf("%d%d", &a, &b);
    int tmp;
    if(a<b) {
        tmp=a;
        a=b;
        b=tmp;
    }
    for(int i=b; i>=1; i--) {
        if(a%i==0&&b%i==0) 
        {
            ucln=i;
            break;
        }
    }
    printf("Ucln la: %d\n", ucln);
    printf("Bcnn la: %d", a*b/ucln);
    return 0;
}
