#include <stdio.h>

int main()
{
    int n;
    printf("Nhap n:");
    scanf("%d", &n);
    int a=1, b=1;
    int next;
    for (int i=3; i<=n; i++) {
        next = a+b;
        a = b;
        b = next;
    }
    printf("%d", next);
    return 0;
    
}
