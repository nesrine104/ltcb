#include<stdio.h>

int main()
{
    int n;
    scanf("%d", &n);
    int x = n;
    int i;
    int arr[100];
    if (n==0) printf("0");
    for(i = 0; x > 0; i++) {
        arr[i]=x%2;
        x/=2;
        
    }
    for(i=i-1; i>=0;i--) {
        printf("%d", arr[i]);
    }
    return 0;
}
