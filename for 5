#include<stdio.h>

int main() 
{
    int a;
    printf("Nhap so a: "); 
    scanf("%d", &a);
    printf("Cac uoc cua %d la: ", a);
    for(int i=1; i<=a; i++) {
        if(a%i==0) printf("%d ", i);
    }
    return 0;
}
