#include<stdio.h>

int main()
{
    int arr[100];
    int n;
    int vt1, vt2;
    int max, min;
    printf("Nhap so phan tu: ");
    scanf("%d", &n);
    
    for(int i=0; i<n; i++) {
        printf("a[%d]=", i);
        scanf("%d", &arr[i]);
    }
    max=arr[0], min=arr[0];
    for(int i=0; i<n; i++) {
        if (arr[i]>max) {
            max=arr[i];
            vt1=i;
        }
        if (arr[i]<min) {
            min=arr[i];
            vt2=i;
        }
    }
    printf("So lon nhat la: %d o vi tri a[%d]\n", max, vt1);
    printf("So nho nhat la: %d o vi tri a[%d]", min, vt2);
    return 0;
}
