#include <stdio.h>

int main()
{
    int n;
    scanf("%d", &n);
    int S = 0;
    for (int i = 1; i <= n; i++) {
        if (n%i == 0)
            S += i;
            
    }
    if (S == 2*n)
        printf("n la so hoan hao");

    return 0;
}
